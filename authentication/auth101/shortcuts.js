// you can add static methods to your Schema in schema.statics, using these methods you can compare the password with the hashedPassword


// we can define a pre(save )mongoose middlewear to hash the passwords for us before saving