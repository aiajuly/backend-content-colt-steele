// when a user is logged in, you can set the user_id to the sesssion //
// in protected route if the user_id is in the session then render the protected page, otherwise ask the user to log in or register


// you can set a middlewear that checks if you are logged in or not( check the session) and add that middlewear in your routes